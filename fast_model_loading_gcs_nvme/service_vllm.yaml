name: fast-model-loading-vllm

containerfile: ./Dockerfile

compute_config:
  worker_nodes:
    - instance_type: a2-highgpu-4g-nvidia-a100-40gb-4
      min_nodes: 0
      max_nodes: 4
      market_type: PREFER_SPOT

working_dir: .

cloud:

applications:
- import_path: serve_vllm:app
